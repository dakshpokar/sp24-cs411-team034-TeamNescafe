/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    BASEURL: "https://suitemate.ue.r.appspot.com/",
  },
  images: {
    domains: ["images.pexels.com"],
  },
};

export default nextConfig;
